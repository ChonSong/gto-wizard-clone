# Prevent nested node_modules — @playwright/test is hoisted from workspace root.
# Running `npm install` here would create a duplicate copy that conflicts with
# the root workspace resolution. Always run `npm install` from the repo root.
ignore-scripts=true
